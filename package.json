{
  "name": "ghl-stripe-webhook",
  "version": "1.0.0",
  "description": "GoHighLevel webhook server that creates Stripe invoices",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.18.2",
    "puppeteer": "^24.42.0",
    "stripe": "^14.21.0"
  }
}
